{
  "name": "blind_social",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "lint": "echo 'No TypeScript to lint'",
    "build": "echo 'No TypeScript to build'",
    "dev": "echo 'Starting dev server...'"
  },
  "dependencies": {
    "pngjs": "^7.0.0",
    "selenium": "^2.20.0",
    "webdriver-manager": "^12.1.9"
  }
}
